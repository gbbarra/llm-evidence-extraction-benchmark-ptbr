# perturbados: gerados localmente, selados até a correção (ver protocolo §6)
corpus/perturbados/
dados/*/perturbacoes-*.json

__pycache__/
*.pyc
saidas-tmp/

# texto plano dos primarios: derivado localmente dos XML (ND do Castro; gerar com perturbar.py)
corpus/primarios-texto/
dados/estudo1/painel-*.html

# estrato fechado: obtidos legalmente (acesso institucional/manuscritos de autor); NUNCA versionar
corpus/fechados-staging/
corpus/fechados-texto/
corpus/perturbados-fechados/
dados/*/perturbacoes-fechados*.json

# estudo3: mesmo regime dos estudos anteriores
corpus/estudo3/primarios-texto/
corpus/estudo3/perturbados/
corpus/estudo3/fechados-staging/
corpus/estudo3/fechados-texto/
dados/*/sementes-auditoria.json

# ancora 3 (azul de metileno): XML dos artigos, rebaixaveis do Europe PMC; nao versionar
dados/estudo11/*.xml
dados/estudo11/primarios/

# estudo12 / ancora 3: mesmo regime das ancoras 1 e 2, e pela mesma razao. Publicar o corpus
# ORIGINAL ao lado do PERTURBADO e publicar o mapa selado: um diff reconstroi o selo inteiro e a
# prova de leitura vira decoracao. Alem disso o texto plano de kirov, memis e levin e artigo de
# acesso fechado, que o regime do estrato fechado manda nunca versionar. So o SHA-256 do selo e
# publico; corpus e mapa vivem locais e se regeneram com e12-perturbar-a3.py.
corpus/estudo12/original/
corpus/estudo12/perturbados/
