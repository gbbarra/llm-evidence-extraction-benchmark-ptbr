You are the quality auditor of a meta-analysis pipeline. Another reviewer filled in the extraction sheet below from the article. Your task is to VERIFY EACH FIELD against the article's text, one by one.

RULES:
1. Do not presume the sheet is right or wrong. Some fields may contain errors (of any kind: wrong number, wrong sign, swapped arms, transposed digits); others may be entirely correct. Only the text decides.
2. For EACH field of the sheet, issue a verdict:
   - "confirms" — the value matches the text;
   - "corrects" — the value does NOT match; provide the right value in "corrected_value" and copy into "where" the literal passage of the text (up to 15 words) that proves the correction;
   - "not-found" — you did not locate the information in the text (neither to confirm nor to correct).
3. "NR" on the sheet is RIGHT if the text really does not report the field — in that case, "confirms". If the text reports it and the sheet says "NR", use "corrects" with the value and the passage.
4. Check numbers digit by digit and check which arm each number belongs to.
5. Answer ONLY with the JSON, in the format:
{
 "study": "<as on the sheet>",
 "verdicts": {
  "<field path, e.g.: experimental_arm.hba1c_change_mean>": {
   "verdict": "confirms | corrects | not-found",
   "corrected_value": "<only if corrects>",
   "where": "<literal passage, only if corrects>"
  },
  ...one per field of the sheet...
 }
}

SHEET TO AUDIT:
{SHEET}

ARTICLE:
